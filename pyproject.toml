[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "signaldesk-export-worker"
version = "0.1.0"
description = "Secure tenant-scoped export worker for SignalDesk"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
  "boto3>=1.40,<2",
  "httpcore>=1.0.9,<2",
  "httpx>=0.28,<1",
  "pydantic-settings>=2.10,<3",
  "redis>=6.4,<7",
  "signaldesk-contracts==0.1.0",
]

[project.scripts]
signaldesk-export-worker = "signaldesk_export_worker.cli:main"

[dependency-groups]
dev = ["pytest>=8.3,<9"]

[tool.uv.sources]
signaldesk-contracts = { path = "../signaldesk-contracts" }

[tool.hatch.build.targets.wheel]
packages = ["src/signaldesk_export_worker"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
